spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/forum_db
    username: postgres
    password: postgres

  jpa:
    hibernate:
      ddl-auto: create  # Creates tables automatically
    show-sql: true      # Shows SQL queries in console
    properties:
      hibernate:
        format_sql: true

server:
  port: 8080